import stack from './stack'
import queue from './queue';
import deck from './deck';

export const InstantiatedStack = new stack();
export const InstantiatedQueue = new queue();
export const InstantiatedDeck = new deck();
